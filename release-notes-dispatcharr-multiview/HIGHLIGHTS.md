**Multiview v0.4.2**

- New **Render Device** setting for QSV and VAAPI hardware encoding. If your box has more than one GPU, you can now pick which render node to encode on instead of always using the first one detected. Leave it on Auto to keep the current behavior.

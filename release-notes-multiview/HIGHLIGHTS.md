**v0.2.2**

- Each multiview layout can now pull real EPG data from one of its source channels - pick "Forward from channel" in EPG settings and choose a channel. Falls back to placeholder if the channel has no EPG data.
- Fixed an intermittent plugin load failure that could occur when Dispatcharr reloads the plugin.
